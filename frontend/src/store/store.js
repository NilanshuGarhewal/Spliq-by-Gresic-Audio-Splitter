// import create from "zustand";

// import { devtools, persist } from "zustand/middleware";

// const fileStore = (set) => ({
//   file: [],
//   addFile: (file) => {
//     set((state) => ({
//       files: [file, ...state.files],
//     }));
//   },
//   removeFile: (fileId) => ({
//     set((state)=> ({
//         files: state.files.filter((e) => e.id !== fileId)
//     }))
//   })
// });
